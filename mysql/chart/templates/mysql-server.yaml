apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: 3306
      targetPort: 3306
      nodePort: {{ .Values.service.nodePort }}

